#lang scheme

(define (divnum x num)
    (/ x num))

(define (check numero)
    (define x
    (- numero 1))
    (if (= x 0)
        #t
        #f))

(define lista '())

(define (mazo cartas divisor)
    body)